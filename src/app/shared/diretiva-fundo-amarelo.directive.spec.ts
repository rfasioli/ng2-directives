import { DiretivaFundoAmareloDirective } from './diretiva-fundo-amarelo.directive';

describe('DiretivaFundoAmareloDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaFundoAmareloDirective();
    expect(directive).toBeTruthy();
  });
});
